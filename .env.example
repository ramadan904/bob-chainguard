# ============================================
# WATSONX HACKATHON - ENVIRONMENT VARIABLES
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (which is in .gitignore)
#    Command: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit the .env file to git
# 4. NEVER share your credentials in code or with AI assistants
#
# ============================================

# IBM Cloud API Key
# Get from: https://cloud.ibm.com/iam/apikeys
IBM_CLOUD_API_KEY=your_api_key_here_DO_NOT_COMMIT


# ============================================
# SECURITY REMINDERS:
# - This .env.example file is safe to commit
# - Your actual .env file is in .gitignore
# - Never hardcode credentials in your code
# - Use process.env.VARIABLE_NAME to access values
# - Never share credentials with AI assistants like Bob
# ============================================
# ============================================
# LEGACY DAPP (Vite exposes only VITE_* vars to the browser)
# ============================================
# Public read-only RPC endpoint. If it embeds a provider key, keep it in .env only.
VITE_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# ERC-20 contract the demo reads/transfers (Sepolia LINK by default)
VITE_ERC20_ADDRESS=0x779877A7B0D9E8603169DdbD7836e478b4624789
# NEVER put a wallet private key or mnemonic in this project. The dApp signs via the injected browser wallet.
