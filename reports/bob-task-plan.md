# Bob migration task plan

Generated by chainguard: 72 legacy call sites, 6 tasks in 3 waves.

Waves follow the import graph: a file is migrated only after every flagged file it imports.
Tasks inside one wave touch disjoint files and do not depend on each other, so give each one
to its own Bob subagent / parallel task. Between waves run `npm run scan` and `npm test`.

## Wave 1 (2 parallel tasks)

### w1-lib-1 (16 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/lib/address.js @legacy-dapp/src/lib/clients.js @legacy-dapp/src/lib/signing.js
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 16 legacy call sites: ETH001 ethers import, ETH002 Web3Provider (browser wallet), ETH003 JsonRpcProvider / StaticJsonRpcProvider, ETH007 ethers.utils.*, ETH010 ethers.constants, ETH014 signMessage via signer, W3J001 web3 import, W3J002 new Web3(), W3J005 web3.utils.*.
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```

### w1-lib-2 (15 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/lib/units.js
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 15 legacy call sites: ETH001 ethers import, ETH004 BigNumber, ETH007 ethers.utils.*, ETH008 BigNumber instance method.
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```

## Wave 2 (2 parallel tasks)

### w2-lib (29 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/lib/erc20.js @legacy-dapp/src/lib/events.js @legacy-dapp/src/lib/gas.js
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 29 legacy call sites: ETH001 ethers import, ETH004 BigNumber, ETH006 ethers Contract instance, ETH008 BigNumber instance method, ETH009 contract/provider event listener, ETH010 ethers.constants, ETH011 tx.wait() / waitForTransaction, ETH012 callStatic / populateTransaction / estimateGas namespace, W3J003 web3.eth.Contract, W3J004 contract.methods.x().call/send, W3J005 web3.utils.*, W3J006 web3.eth RPC call, W3J008 getPastEvents.
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```

### w2-hooks (8 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/hooks/useWallet.js
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 8 legacy call sites: ETH001 ethers import, ETH005 getSigner(), ETH007 ethers.utils.*, ETH013 ENS lookup via provider, ETH015 raw provider RPC (send / listAccounts / getNetwork).
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```

## Wave 3 (2 parallel tasks)

### w3-hooks (3 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/hooks/useErc20.js
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 3 legacy call sites: ETH001 ethers import, ETH004 BigNumber, ETH008 BigNumber instance method.
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```

### w3-components (1 findings)

```text
Migrate these files from ethers v5 / web3.js to viem + wagmi: @legacy-dapp/src/components/TransferForm.jsx
Follow @docs/MIGRATION_PLAYBOOK.md (mapping table and rules). Do not edit any other file.
chainguard found 1 legacy call sites: ETH016 ethers error shape (err.reason).
Keep every exported name and call signature stable. Raw amounts become bigint; functions that returned strings still return strings.
Done when `node chainguard/bin/chainguard.js scan legacy-dapp/src` lists none of these files and `npm test --prefix legacy-dapp` passes.
```
